public without sharing class IITDoNothing {
//Do Nothing section
@InvocableMethod
	public static void doNothing(List<String> doNothingStrings) {//Invocable from Process Builder, does nothing, so process builder can exit early
	}
}