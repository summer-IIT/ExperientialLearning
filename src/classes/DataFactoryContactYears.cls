@isTest
public without sharing class DataFactoryContactYears {
    public static List<Integer> createContactYears() { 
        // Creates a list of contact years for use
        List<Integer> contactYears = new List<Integer>();
        contactYears.add(2014);
        contactYears.add(2015);
        contactYears.add(2016);
        contactYears.add(2017);
        return contactYears;
    }
}